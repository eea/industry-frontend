const schema = {
  title: 'Industry map',

  fieldsets: [
    {
      id: 'default',
      title: 'Default',
      fields: [],
    },
  ],

  properties: {},

  required: [],
};

export default schema;
