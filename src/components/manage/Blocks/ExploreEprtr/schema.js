const Schema = {
  title: 'Explore the data',

  fieldsets: [
    {
      id: 'default',
      title: 'Default',
      fields: [],
    },
  ],

  properties: {},

  required: [],
};

export default Schema;
