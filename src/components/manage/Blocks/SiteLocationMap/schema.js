const schema = {
  title: 'Site location',

  fieldsets: [
    {
      id: 'default',
      title: 'Default',
      fields: [],
    },
  ],

  properties: {},

  required: [],
};

export default schema;
