const getSchema = (props) => {
  return {
    title: 'Environmental facility details',

    fieldsets: [
      {
        id: 'default',
        title: 'Default',
        fields: [],
      },
    ],

    properties: {},

    required: [],
  };
};

export default getSchema;
